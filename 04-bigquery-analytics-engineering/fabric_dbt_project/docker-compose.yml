version: '3.8'
services:
  # dbt-bq-dtc:
  #   build:
  #     context: .
  #     target: dbt-bigquery
  #   image: dbt-bigquery
  #   volumes:
  #     - .:/usr/app
  #     - ~/.dbt/:root/.dbt/
  #     - ~/.google/credentials/google_credentials.json:/.google/credentials/google_credentials.json
  #   network_mode: host

  dbt-fabric-dtc:
    build:
      context: .
      target: dbt-fabric
    image: dbt-fabric
    volumes:
      - .:/usr/app
      - ~/.dbt/:root/.dbt/
      - ./profiles.yml:/root/.dbt/dbt_project.yml
    network_mode: host